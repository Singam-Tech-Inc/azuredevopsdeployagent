services:
  azdevops-agent:
    build:
      context: .
      args:
        TARGETARCH: ${TARGETARCH:-amd64}
    image: ${IMAGE_REPO:-azdevopsdeploymentagent}:${VERSION:-1.0}-${TARGETARCH:-amd64}
    container_name: azdevops-agent
    restart: always
    volumes:
      - deploymentdevagent:/azagent
    environment:
      - AZP_URL=${AZP_URL}
      - AZP_TOKEN=${AZP_TOKEN}
      - AZP_PROJECT=${AZP_PROJECT}
      - AZP_ENVIRONMENT=${AZP_ENVIRONMENT:-development}
      - AZP_AGENT_NAME=${AZP_AGENT_NAME:-agent}
    networks:
      - agent_network

volumes:
  deploymentdevagent:
    external: true

networks:
  agent_network:
    external: true
    name: ${NETWORK_NAME:-deployagent_default}
